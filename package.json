{
  "name": "game",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "compile": "browserify src/Game.js -o build/main.js -t [ babelify --presets [ es2015 ] ]",
    "debug": "browserify src/Game.js -o build/main.js -t [ babelify --presets [ es2015 ] ] -d"
  },
  "author": "jokly",
  "license": "ISC",
  "dependencies": {
    "gl-matrix": "^2.3.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "watchify": "latest"
  }
}
